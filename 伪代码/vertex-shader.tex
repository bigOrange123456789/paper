\documentclass[11pt]{ctexart}  
\usepackage[top=1cm, left=1cm, right=1cm]{geometry}  
\usepackage{algorithm}  
\usepackage{algorithmicx}  
\usepackage{algpseudocode}  
\usepackage{amsmath}  

\floatname{algorithm}{algorithm}  
\renewcommand{\algorithmicrequire}{\textbf{InputData:}}   
\renewcommand{\algorithmicensure}{\textbf{OutData:}}  

\begin{document}  
	\begin{algorithm}  
		\caption{vertexfragment}  
		\begin{algorithmic}[1]  %每行显示行号  
			
			\Require 	
			\State
			vertexInf=\{ $position$，$UV$，$skinIndex$   \}//Information about the current vertex \\
			instanceObjInf=\{  //Information about the instantiated objects\\
				\qquad $matrix$,\\
				\qquad $animationStyle$,//The type and speed of animation\\
				\qquad $textureStyle$,//Types and tones of maps\\
			\}
			\State
			uniformInf=\{  //Data shared by all instantiated objects\\
			\qquad $skelontonDate$,//Bone data\\
			\qquad $time$,//Time is used to calculate the current frame number\\
			\}
			
			 \State   
			
			  
			  
			  
			  
			  %matrix2
			  \If {$bone(vertexInf.skinIndex) have \; animation$}
			  \State $frameIndex=time*speed \ mod (frameIndexMax+1);$
			  \State $address0=addressGet1(skinIndex,type,frameIndex);$
			  \Else  
			  \State $address0=addressGet2(skinIndex,type);$
			  \EndIf   
		\State $matrix1 \gets skelontonDate \ at \ position \ address0 ;$
			  
			  \State  $matrix2 \gets instanceObjInf.matrix;$ 
			  
			  \State  $glPosition = modelViewProjectionMatrix*matrix2*matrix1*position;$
			  
			\State $judgeArea();$ //Determine which part of the body the current vertex is in

			  
			
			\Ensure
			\State
			glPosition //The current vertex corresponds to the position on the screen \\
			sendFragmentShader=\{  //Information sent to fragmentshader\\
			\qquad $areaType$,//The area where the current point is located\\
			\qquad $UV$,
			\qquad $textureType$,//Map type\\
			\qquad $color$,//For tone adjustment\\
			\}
			
		\end{algorithmic}  
	\end{algorithm}  
\end{document}   